// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("fanta", "for fanta support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("fanta", "enable fanta support", "no");

if (PHP_FANTA != "no") {
	EXTENSION("fanta", "fanta.c");
}

